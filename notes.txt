start with gp 
simulate a sample path for y 
fit new gp with same kernel but different hyper parameters
see if it can estimate previous kernel's (bias)
    try modifying starting vals
    modify restarts 


sanity check for sigma^h based on the 2009 paper sigma^h(x, x) = 1 for all x in S^d - 1